---
# Playbook: Performance tuning for all DingoFS nodes
# Configures: CPU governor, ulimits, sysctl, hugepages, cpupower idle
- name: Apply performance tuning to DingoFS nodes
  hosts: meta_servers
  remote_user: root
  become: true
  roles:
    - tune
