---
# Playbook: Performance tuning for DingoFS cache nodes
# Reuses the existing tune role (CPU governor, ulimits, sysctl, hugepages, cpupower)
- name: Apply performance tuning to cache nodes
  hosts: cache_servers
  remote_user: root
  become: true
  roles:
    - tune
